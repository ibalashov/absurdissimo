// Reuse the admin form controls and pager shared by the other data sections.
import "../starter-packs/starter-packs.css";

export default function WordInfoLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return children;
}
